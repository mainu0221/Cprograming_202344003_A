제곱과 제곱근 함수
```c
#include <stdio.h>
#include <math.h> //수학 함수를 위해 선언

int main(void){

    printf("10의 3제곱은 %.0f\n", pow(10, 3)); //제곱을 구하는 함수
    printf("16의 제곱근은 %.0f\n", sqrt(16)); //sqrt 제곱근 구하는 함수

    return 0;
} 
```
